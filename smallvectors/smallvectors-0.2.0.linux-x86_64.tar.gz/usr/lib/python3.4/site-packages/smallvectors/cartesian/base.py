# -*- coding: utf8 -*-
'''
Module that implements Vec, Point and Direction base types
'''

import six
from smallvectors.core import VecOrPointMeta
from smallvectors.generics import (
    add, sub, mul, truediv,
    add_safe, sub_safe, mul_safe, truediv_safe,
    promote, promote_list, set_promotion_function, convert_list,
    GenericObject,
)

VALID_ELEMENT_TYPES = set()
FORBIDDEN_ELTYPES = {list, tuple, str}

#
# Some aliases
#


###############################################################################
#                               Vector types
###############################################################################


###############################################################################
# Point types
###############################################################################


if __name__ == '__main__':
    import doctest
    doctest.testmod()
    from smallvectors import Vec
